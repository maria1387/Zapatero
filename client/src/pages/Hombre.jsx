// import { useContext } from "react";
// import { ZapateroContext } from "../context/ZapateroProvider";

const Hombre = () => {
// 	const { ZapateroData } = useContext(ZapateroContext);
 
// //   console.log(id);

//   const ZapateroHombre = ZapateroData.filter((zapatero) => (zapatero.categoria.hombre) ==="hombre")
	return (
		<div>
			{/* {
			ZapateroHombre?.map((item, index) => {
				if (item.mujer = "Mujer") {
					// item.adulto = true;
					} else if(item.hombre = "Hombre") {
					// cliente.adulto = false;
					
				} else (item.ninos = "ninos") {
					// cliente.adulto = false;
					}
					

				})
				// <Card item= {item} key={index}
				
				} */}
	</div>
	)
};

export default Hombre;
